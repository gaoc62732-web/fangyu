# 手册版本兼容测试

本包地图为**空白审核版**，与浏览器助手共用同一基础名录指纹。需先完整解压，再通过“先打开这里.html”进入。

## Excel → 地图

打开 `工具/方舆Excel导入助手_兼容版.html`，选择手册 XLSX。助手按政区名称、区划代码与内容项名称逐项迁移 `√`，自动补勾上级政区；旧版名录不会整格覆盖新版站名、博物馆等基础内容。下载 JSON 后，在本包地图中点击“导入记录”。未匹配条目可单独下载 CSV 核对。处理均在本机浏览器完成。

## 地图 → Excel

从本包地图导出记录 JSON，再从地图导出一个空白 Excel 作为基础工作簿，执行：

```powershell
python 工具/merge_site_to_excel.py --records 记录.json --base 空白手册.xlsx --out 合并手册.xlsx
```

该步骤需要 Python 及 `openpyxl`。脚本按代码和政区名称定位行，遇到不属于该政区层级的单元格或无法唯一定位的旧版政区时跳过并列明；原文件不被覆盖。

## 本次实测

| 输入版本 | 已识别内容项标记 | 可迁移单元格 | 未唯一匹配条目 |
| --- | ---: | ---: | ---: |
| 2026-09-25 合并版 | 3696 | 4540 | 9 |
| 2026-09-17 修订版 | 3696 | 4540 | 9 |
| 2020 标准版 v2.0.2005 | 3219 | 4051 | 131 |

2020 版有 42 个旧区划代码不能直接按代码对应，其中 6 行可按名称唯一对应。其余旧政区标记或已改名、迁移、缺失的内容项进入未匹配清单，不会猜测。三个版本的全部导入值均已验证：去除新增 `√` 后，文字与本包基础名录逐字相同。反向测试：旧版记录合并进新版工作簿应用 202 格、跳过 0 格；尝试写回 2020 工作簿时，27 格因旧版没有对应的新政区或名称发生变化而明确跳过。台湾重复区划代码的错位写入已被拦截。

地图空白状态、三个层级 PNG、CSV/Excel 导出、100 个公开车站测试导入和撤销、照片 GPS 导入、重新打开保持空白，均已通过自动检查。ZIP 未收录上述测试产生的记录 JSON 或任何个人到访工作簿。
